import React from 'react'
import { useSelector } from 'react-redux'

export default function FilterCard({ data, getAllOrders, index }) {
    const userInfo = useSelector(state => state.user.userInfo)
    return (
        <>

        </>
    )
}
